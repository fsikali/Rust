// Enum Definitions
// We can define enums too hold generic in their variants 

#![allow(unused)]  

enum Option<T> { 
    Some(T), 
    None,
} 

pub fn my_data_type() { 
} 

// By using the the Option<T> enum we can express the abstract concept of an optional value 
