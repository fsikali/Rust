// Structs give away of grouping together fields and data
//  Struct or structure is a custom data type that 
// lets you package together and name multiple related values that make up a meaningful group 

// Struct update syntax

/* 
struct House { 
    length: u64,     // fields and values
    width: u64, 
    house_name: String, 
    built: bool,
} 

fn person_one(name: String, email: String) -> Person { 
    Person {  
        // using the field init shorthand
        name, 
        email, 
        travelled: true, 
        id: 3000,
    } 

    Person

}

pub fn my_struct() {  
    let house_a = House { 
        length: 100, 
        width: 40, 
        house_name: String::from("Skyview"),
        built: true,
    }; 

    let the_person = person_one( String::from("Flemming"), 
     String::from("flemmingsikali499@gmail.com")); 

     println!("{the_person}");
 
} 

*/ 

pub fn my_struct() { 
    
}