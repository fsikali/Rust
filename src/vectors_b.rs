// Rust conveniently provides the vec! macro, which will create a new vector that
// holds the values you give it

pub fn my_vector() {  

    let v = vec![1, 2, 3];
} 

// Rust infers the type of value you want to store
// Type annotation isn't necessary
//
//